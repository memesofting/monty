#include "monty.h"
/**
 * me_stack - prints the top
 * @head: stack head
 * @counter: line_number
 * @bus: bus struct
 * Return: no return
*/
void me_stack(stack_t **head, unsigned int counter, bus_t *bus)
{
	(void)head;
	(void)counter;
	bus->lifi = 0;
}
